import Hero from "../components/Hero";
import Services from "../components/Services";

const Base: React.FC = () => {
  return (
    <div>
      <Hero />
      <Services />
    </div>
  );
};

export default Base;
